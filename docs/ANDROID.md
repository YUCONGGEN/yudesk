# YuDesk Android 2.0.0-preview.13

Android 端采用原生 Java 界面、Go 通信核心和原生 WebRTC。一个 APK 可以控制电脑或 Android，也可以在手机持有人明确授权后共享手机屏幕、接受远程触控和参加多人会议。iOS 与原生鸿蒙当前暂停。

## 当前交付

`preview.13` 的版本代码为 `2000013`，最低 Android 8.0 / API 26，包含 `arm64-v8a`、`armeabi-v7a`、`x86_64` 和 `x86` 四种 ABI。签名证书 SHA-256：

```text
c8e35f5904ad92ab55a651f940d531c1193042dcfc2ac1a98b7dadf0c3994a6a
```

官网 APK 为 43,859,825 字节，SHA-256：

```text
b93fe2b80994712f37975d6876f874e5be44c905a0d664072dd85098bb82a2ca
```

该版本保留 Android 厂商启动时序修复：系统栏操作在 `setContentView` 之后执行，并先确保 `DecorView` 已创建，避免部分 Android 11+ 系统在冷启动时因空 `WindowInsetsController` 直接退出。首页在系统状态栏/刘海安全区之外增加 22dp 顶部留白，不再紧贴屏幕顶边；远控和会议仍使用独立的横屏沉浸式全屏布局。覆盖安装会保留应用私有设备身份；签名不一致时 Android 会拒绝覆盖，不能通过自动卸载规避，因为卸载会丢失身份与设置。

这是预览版，不是“全部安卓手机均已验收”的正式版。厂商后台策略、安全桌面、系统应用、DRM 和 `FLAG_SECURE` 内容可能限制共享或触控；YuDesk 不绕过系统锁屏或安全限制。

## 远程控制

1. 从官网安装 APK，打开后显示 9 位设备码和 6 位 PIN。设备需由管理后台授权，或服务器开启免激活。
2. 作为被控端，点击“授权共享屏幕”，允许通知并在 Android 系统 MediaProjection 窗口中确认。每次重新共享都需要系统授权。
3. 需要对方操作手机时，点击“开启远程触控权限”，由手机持有人进入系统无障碍设置并启用 YuDesk；仅观看不需要该权限。
4. 作为控制端，输入另一台设备的 9 位设备码。PIN 可填写，也可留空等待对方在 60 秒内同意；错误 PIN 不会自动降级为审批。
5. 控制电脑时默认使用模拟鼠标：单指移动指针、轻点左击、连续轻点双击、双指滚动，并提供左右键、拖动、文字、返回与桌面操作。触屏模式按画面绝对坐标点选。远程与会议均支持横屏沉浸式全屏。
6. “结束”或手机返回键只结束当前会话并回到 YuDesk 首页；主界面“退出”停止接收、停止共享并关闭应用。

Android 自身的系统“返回”和“主页”不会误发送到远端。工具栏中的远端返回/桌面动作按目标平台转换：Windows 使用 Alt+左方向键与 Win+D，Android 使用对端已经授权的无障碍全局操作。中文文字使用 UTF-8/Java Unicode，不借用命令行编码。

设备历史只保存最近设备码，不保存 PIN。更换 PIN 后会通过固定服务器证书的 TLS 管理通道同步。管理员禁用、断开、删除或授权到期会终止连接；明确拒绝后不会紧密循环重连。

## 多人会议

- 填写 1～32 个字符的姓名后，可发起会议或输入 9 位会议号直接加入；
- 发起人也会进入会议，并成为第一位主持人；
- 最多 8 人，支持麦克风、前置摄像头、成员列表、主持人共享屏幕、转交主持人和结束会议；
- 远端会议音轨会显式启用并默认路由至手机扬声器；工具栏可关闭声音或再次播放声音；
- 参会者不需要主持人二次确认；
- Android 会议自动进入横屏全屏，退出后恢复原方向；
- 会议媒体使用原生 WebRTC/ICE/DTLS-SRTP，优先设备直连；3 秒无法建立媒体路径时自动使用服务器下发的短期 TURN 凭据，服务器只转发加密数据包；
- Android 本地录制当前视频/屏幕画面，暂不包含会议或系统音频；
- 主持人共享 Android 屏幕时仍会显示真实系统 MediaProjection 授权窗口和持续通知。

Android 的屏幕共享当前发送画面并保留麦克风通话，不采集其他应用的内部系统声音；这是与桌面“共享系统音频”的平台差异。

当前会议是最多 8 人的 P2P 优先 mesh，直连失败会自动回退 TURN，但没有 SFU。人数增多会增加每台手机的上行、下行、编解码、发热和耗电。详情见 [多人会议说明](CONFERENCE_20260911.md)。

## 功能边界

| 功能 | 状态 |
| --- | --- |
| Android ↔ PC、Android ↔ Android | 共用身份、授权、远程协议；API 26 模拟器和 Windows/Android 公网互通已验证，物理真机仍需验收 |
| 空 PIN 审批、正确 PIN 连接 | 支持；60 秒、一次性、限流，观看模式拒绝输入 |
| 屏幕共享 | MediaProjection，每次系统授权，前台服务和停止通知 |
| 远程触控 | AccessibilityService；点击、拖动、滚动、文字和受支持系统操作 |
| 横屏全屏 | 普通远程与会议均支持，可主动切换方向 |
| 多人会议 | 最多 8 人；麦克风、摄像头、主持共享、成员、转主持人和本地录制 |
| Android 系统声音 | 未实现 |
| Android 会议录音 | 未实现；当前录制只保存视频/屏幕 |
| Android 文件传输、剪贴板同步 | 未实现，不提供无效按钮 |
| 受保护桌面、DRM、锁屏绕过 | 不支持 |
| H.264/AV1 远程桌面硬件编码 | 未实现，不能宣称达到商业产品的全部性能 |

## 低延迟与稳定性

远程桌面的采集/压缩、网络写入、网络读取、触控注入和解码渲染使用独立的有界通道。采集采用 `acquireLatestImage`，最多保留一个待发完整画面；输入移动事件可合并，但按下、拖动、抬起和键盘事件不跨边界丢弃。Bitmap 仅保留最新已提交帧，避免 RenderThread 仍引用时过早回收。

远程桌面预览路径以最长边 1280、JPEG 75、最高 30 FPS 和约 8 Mbps 画面预算为基线；实际帧率取决于屏幕变化、CPU、编码和网络，不保证固定 30 帧或零延迟。会议使用原生 WebRTC 自带的拥塞控制、抖动缓冲、回声消除、降噪和自动增益；网络 RTT 的物理传播时间无法由多线程消除。

本批次修复了无媒体入会时空 SDP 与 `max-bundle` 冲突、TLS 合并首条欢迎消息导致超时、成员离开后迟到 ICE 误断开其他成员，以及 Windows/Android 跨严格 NAT 时只有成员状态却没有音频路径的问题。Android 显式使用 `JavaAudioDeviceModule` 启动录音与播放，启用硬件回声消除和降噪，并把通话默认路由到扬声器。Go 核心三轮竞态测试、Java 单测、Android lint、四 ABI 构建、APK v2 签名、16 KiB ZIP/ELF 对齐和 API 26 模拟器冷启动均通过。

## 构建

Android 子模块位于 `mobile/`，使用 Go 1.27.1、JDK 17、Android SDK 35、Build Tools 35.0.0 和 NDK 27.3.13750724。构建脚本：

```sh
export ANDROID_HOME=/path/to/android-sdk
export ANDROID_NDK_HOME="$ANDROID_HOME/ndk/27.3.13750724"
bash mobile/scripts/build-android.sh
```

脚本固定 gomobile/gobind、AGP 与 Gradle 版本，并执行 Go race/vet、Java 单测、lint、签名和 16 KiB 对齐检查。GitHub Actions 只构建预览 APK，不自动发布，也不提交签名私钥。

## 真机验收清单

- Android 8/10/13/14/15/16，至少覆盖两种以上厂商和一台 16 KiB 页面设备；
- 手机控制 PC、PC 控制手机、两台手机互控；分别验证 PIN、无 PIN 同意/拒绝/超时和管理员停止；
- 会议 2～8 人、麦克风、摄像头、旋转、主持共享、转主持人、离会、迟到 ICE、录制文件；
- 权限取消、通知拒绝、无障碍撤销、锁屏/解锁、切后台、划掉任务、网络切换；
- 连续 30 分钟滚动/视频/会议，记录 FPS、RTT、输入到画面延迟、内存、温升和耗电。

只有完成对应物理设备测试，才能把该机型列为已验证兼容。
